# Copy this file to Signing.local.mk (ignored by git), then fill in only the
# values needed for the target you intend to run. CI may provide the same names
# as environment variables instead.

LOCAL_CODESIGN_IDENTITY ?=
DIST_CODESIGN_IDENTITY ?=
DIST_TEAM_ID ?=
NOTARY_PROFILE ?=
